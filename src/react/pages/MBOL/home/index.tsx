import React, { useState, useEffect } from "react";
import { useTranslation } from "react-i18next";
import styled from "styled-components";

const HomeMBOLPage: React.FC = () => {
  const { t, i18n } = useTranslation();

  return <></>;
};

export default HomeMBOLPage;
